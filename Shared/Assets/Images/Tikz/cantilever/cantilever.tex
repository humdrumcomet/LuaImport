% arara: lualatex
% arara: lualatex

\providecommand{\sharedPath}{../../../../../Shared}
\providecommand{\importPath}{\sharedPath/Imports}
\providecommand{\assetPath}{\sharedPath/Assets}
\providecommand{\codePath}{\sharedPath/Assets/LuaCode}

\documentclass[tikz, crop=false]{standalone}

\input{\importPath/preambleGen}
\input{\importPath/glossary}
\input{\importPath/symbols}
\input{\importPath/assets}
\input{\importPath/equations}
%-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

\directlua{
    path = "\codePath"
    ass1 = require(path.."/Assignment1")
}

\begin{document}
    \begin{tikzpicture}[
        test/.pic={\draw (0,0) rectangle (2,3);}
        ]
        \matrix
        {
            \node(m){};
            \draw(m) rectangle ++(2,3);
            & 
            \node(b){};
            \draw(b) rectangle ++(2,3);\\
        };
        %\draw (11.1,5.5) pic {test};
        %\begin{scope}[every path/.style=line]
            %\path (a1) -- (a2);
            %\path (a2) -- (b1);
            %\path (b1) -- (b2);
        %\end{scope}
    \end{tikzpicture}
\end{document}
